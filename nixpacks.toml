[phases.setup]
nixPkgs = ["python312", "libpq"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "export PATH=$HOME/.local/bin:$PATH && uv sync --frozen --compile-bytecode"
]

[start]
cmd = "uv run uvicorn app:asgi --host 0.0.0.0 --port $PORT"
