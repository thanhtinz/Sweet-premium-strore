[build]
builder = "nixpacks"

[deploy]
startCommand = "uv run uvicorn app:asgi --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.deploy]
numReplicas = 1
